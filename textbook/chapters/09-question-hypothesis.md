# 第 9 章　从研究问题到仿真假设

> 有了智能体、环境、网络、LLM、运行系统——技术层全部到位。但**做研究不是从"我有工具"开始,而是从"我有研究问题"开始**。这一章讨论社会仿真作为研究方法的全流程第一步:如何把一个模糊的研究问题,变成可被仿真验证的、可证伪的、有可测量指标的假设。读完本章,读者应该能用预注册协议的格式写出自己的第一个仿真研究计划。

---

## 9.1　什么适合仿真:复杂机制、多变量、非线性、可重复

不是所有社会现象都适合用仿真研究。社会仿真作为研究方法,有明确的适用边界。

**适合仿真的**:

- **机制识别问题**:同一现象有多个互相竞争的理论,你想知道哪个机制能复现宏观。例如"为什么限行令效果递减",有"替代效应""适应效应""网络效应"三个理论。
- **反事实评估问题**:你想知道"如果当初做了 X 而不是 Y,结果会怎样"。例如"如果限行令没有实施,出行量会继续增长多少"。
- **长期动态问题**:你想知道 5 年、10 年后的趋势。例如"延迟退休政策对劳动力市场的影响"。
- **多变量交互问题**:你关心的现象受 5+ 变量影响,数学建模不可解析。例如"气候政策 × 经济冲击 × 技术变化 × 社会态度"四维交互。
- **不可实验问题**:伦理或现实限制让你无法做真人实验。例如"疫情封控的最优时长"。

**不适合仿真的**:

- **简单关联问题**:两个变量的相关/因果,直接用统计就行。例如"收入和幸福感的关系",仿真反而过度复杂。
- **小样本问题**:样本量 < 30 的现象,统计推断把握度不够,仿真也救不了。
- **历史唯一性问题**:只发生过一次的事件,没有"反事实"可比。例如"拿破仑战争的影响",仿真的反事实不成立。
- **意义建构问题**:现象的核心是"人对意义的赋予",仿真只能逼近不能替代。例如"宗教皈依体验"。
- **价值判断问题**:涉及"应当怎么做",仿真只能给"会发生什么"。例如"减税是否道德"。

读者做研究时,先问自己:**我要研究的问题在哪一类?** 这决定了你应不应该用仿真。

```
                ┌─────────────────────┐
                │   模糊的研究问题    │
                └──────────┬──────────┘
                           │
                ┌──────────┴──────────┐
                │ 这适合仿真吗?       │
                └──────────┬──────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
    适合              边界                不适合
        │                  │                  │
        ▼                  ▼                  ▼
   进入仿真流程      仿真 + 其他方法      用其他方法
   (本章以下)       组合
```

### 一个具体的判断练习

假设你要研究"远程办公对城市碳排放的影响"。这是适合仿真的吗?

- 涉及多个变量:通勤、出行方式、办公建筑能耗、家庭能耗、消费模式…… ✓ 复杂机制
- 可以反事实:有远程办公 vs 无远程办公 ✓ 反事实评估
- 可以长期仿真:10 年、20 年视角 ✓ 长期动态
- 不能直接做实验 ✓ 不可实验
- 多变量交互:政策 × 城市 × 行业 × 家庭 ✓ 多变量

**结论:非常适合仿真**。

---

## 9.2　预注册协议:把假设写成可证伪的形式

社会仿真研究最容易出的问题是**事后解释**——跑完仿真,看到结果,然后声称"这正好证明了 X 理论"。这是伪科学的标准反模式。

避免它的工程方法是**预注册**(pre-registration):在跑仿真之前,把假设、指标、判定规则写下来,跑完之后**只能按预注册的规则判定**,不能临时改假设或指标。

GAWorld 的"研究工作台"实现了完整的预注册流程:

```
[1] 研究问题:用一句话描述
[2] 条件:平行世界的差异(每个世界一个事件表)
[4] 假设:每条假设包含:
    - 指标(从可测量目录选)
    - 两个条件(世界 A vs 世界 B)
    - 方向(支持 H1 / 反对 H1 / 不显著)
    - 最小可观测效应
[5] 跑前检查:指标是否在目录中、调用量是否在预算内
[6] 批准:copilot 模式在这里停下
[7] 跑实验:每个种子 × 一次世界
[8] 判定:每条假设 → supported / contradicted / inconclusive / unmeasured
[9] 报告:预注册在前,结果在后,严格按规则解读
```

### 一个完整的预注册示例

研究问题:**限行令对城市出行行为的影响**。

```
条件(平行世界):
  W1: control(不实施限行)
  W2: limit_weekday(工作日按尾号限行)
  W3: limit_with_subsidy(限行 + 公共交通补贴)

假设:
  H1: 限行令减少私家车出行
      指标: car_trips_per_capita(代表每日人均驾车次数)
      条件对比: W2 vs W1
      方向: car_trips_per_capita(W2) < car_trips_per_capita(W1)
      最小可观测效应: -10%(即限行后人均驾车次数减少至少 10%)

  H2: 限行令 + 补贴增加公共交通客流
      指标: public_transit_users(代表每日公交+地铁客流总量)
      条件对比: W3 vs W2
      方向: public_transit_users(W3) > public_transit_users(W2)
      最小可观测效应: +15%

跑前检查:
  ✓ car_trips_per_capita 在可测量目录中
  ✓ public_transit_users 在可测量目录中
  ✓ 三个世界调用量预算: ~5 万次 LLM 调用,可接受

批准:✓

实验:
  跑 3 个世界 × 5 个种子 = 15 次仿真
  每个仿真: 1000 人 × 180 天

判定:
  H1: 跑完后看 W2 vs W1 的 car_trips_per_capita
      若 W2 减少 ≥ 10% → supported
      若 W2 减少 < 10% 但 > 0 → inconclusive
      若 W2 反而增加 → contradicted
```

这个预注册协议是 GAWorld 研究工作台的"标准模板"。读者做自己的研究时,应该严格按这个格式写——不要临时改假设,不要事后选指标,不要"看到结果再说"。

---

## 9.3　效应量、最小可观测效应与样本量

预注册里有两个关键参数:**最小可观测效应**(Minimum Detectable Effect, MDE)和**样本量**(sample size)。

**效应量**(effect size)是衡量"差异有多大"的统计量。常用指标:

- **Cohen's d**:两组均值差 / 合并标准差。0.2 = 小,0.5 = 中,0.8 = 大。
- **相对差异**:`(mean(B) - mean(A)) / mean(A)`,单位是百分比。
- **绝对差异**:`mean(B) - mean(A)`,单位是指标原值。

**最小可观测效应**(MDE)是你"愿意接受"作为"有效果"的最小效应值。设定太大会漏掉真实的小效应;设定太小会被噪声干扰。GAWorld 的默认规则:

- 收入、消费、出行等高频变量:MDE = 5%
- 态度、情绪等低频变量:MDE = 10%
- 网络结构等结构变量:MDE = 15%

**样本量**(sample size)在这里指"多少个仿真世界"。仿真研究里,样本量 = 世界数 × 种子数。

- 5 个世界 × 3 个种子 = 15 次仿真
- 3 个世界 × 5 个种子 = 15 次仿真

这两种设计的信息不同:**多个世界看条件效应,多个种子看随机变异**。通常**3 条件 × 5 种子**是好的起点——既能看到条件效应,也能估计随机变异。

### 一个 MDE 与样本量的练习

假设你要验证"限行令减少人均驾车 10%":

- 1000 人 × 180 天,每个仿真产生 ~180 × 1000 个 car_trips 数据点
- 3 个种子 × 2 个条件 = 6 次仿真
- 总数据点: ~180 × 1000 × 6 = 108 万

这么大的样本量,统计上几乎肯定能检测到 10% 的差异。但问题是:**这个 10% 在现实里有没有意义?**——1000 人 × 180 天跑出来的 10% 差异,可能对应真实城市 1 个月内的 1% 差异。所以仿真里的"显著性"和现实里的"意义性"是两个不同概念,读者写论文时要把它们都写清楚。

---

## 9.4　对照组、安慰剂与平行世界

仿真的"对照组"有三种形态:

**形态一:对照世界**(control world)。一个完全不做干预的"基线"世界,所有变量都在自然演化。W1 通常就是对照。

**形态二:安慰剂世界**(placebo world)。一个"虚假干预"的世界——比如"宣布但不实施"限行令、或"在另一个不相关领域"实施干预。安慰剂世界用于检测"是不是事件本身的影响,而非事件的内容"。

**形态三:平行世界**(parallel world)。多个不同干预的并行,每个有不同的政策/事件。平行世界用于"哪种政策更有效"。

GAWorld 的实现:

```
--spec worlds.json:
{
  "worlds": [
    {"name": "control", "events": []},
    {"name": "placebo", "events": [{"name": "fake_policy", ...}]},
    {"name": "limit_weekday", "events": [{"name": "limit_weekday", ...}]},
    {"name": "limit_with_subsidy", "events": [
      {"name": "limit_weekday", ...},
      {"name": "transit_subsidy", ...}
    ]}
  ]
}
```

跑完 4 个世界 × 5 个种子,得到 20 次仿真结果。读者分析时:

- 哪个条件平均效应最大?
- 安慰剂效应是否显著?
- 不同种子下的效应是否稳定?

这三条问题决定了"结论可信度"。

---

## 9.5　可测量指标目录

社会仿真需要一个"指标目录"(metrics catalog)——所有可被自动测量的指标清单。GAWorld 的指标目录在 `output/metrics/catalog.json`,目前包含 200+ 指标,分 8 大类:

**经济类**:个人收入、消费、储蓄、负债、净资产、流动性、就业状态
**出行类**:每日出行次数、出行距离、出行方式、通勤时间、机动车使用
**社交类**:每日接触人数、关系强度均值、社交多样性、孤岛度
**心理类**:情绪均值、压力均值、幸福感指数、焦虑指数
**行为类**:决策选择分布、行动频次、活动多样性
**网络类**:网络密度、平均路径长度、聚类系数、连接性
**认知类**:信息量、技能熟练度、学习曲线
**宏观类**:基尼系数、就业率、GDP、增长率、社会信任

读者做研究时,**只能从指标目录里选假设**。不能"想看什么指标就让仿真出什么指标"——这是事后选择,会显著抬高假阳性。

### 一个完整的"假设 → 指标"映射

以限行令案例为例:

| 假设 | 指标 | 条件对比 | 方向 | MDE |
|---|---|---|---|---|
| H1 限行减少私家车出行 | car_trips_per_capita | W2 vs W1 | ↓ | -10% |
| H2 限行 + 补贴增加公交 | public_transit_users | W3 vs W2 | ↑ | +15% |
| H3 限行影响低收人家庭更多 | low_income_car_trips | W2 vs W1 | ↓ | -15% |
| H4 限行增加步行和自行车 | walk_bike_trips | W2 vs W1 | ↑ | +8% |

这种"假设 → 指标"的显式映射,是预注册协议的核心内容。读者写论文时,要严格按这个表执行。

---

## 9.6　一个真实研究的预注册示例:公共图书馆的代际影响

让我们用一个真实的研究问题作为本章的总结——研究"公共图书馆的使用对儿童学业成就的代际影响"。

**研究问题**:父母在儿童时期使用公共图书馆,是否会增加其子女的学业成就?

**预注册协议**:

```
条件(平行世界):
  W1: control(无公共图书馆影响)
  W2: parent_library_low(父母儿童时期低图书馆使用)
  W3: parent_library_high(父母儿童时期高图书馆使用)

假设:
  H1: 父母儿童时期高图书馆使用 → 子女学业成就更高
      指标: child_education_attainment(子女 18 岁时的学历)
      条件对比: W3 vs W1
      方向: child_education_attainment(W3) > child_education_attainment(W1)
      MDE: +0.5 个学历等级(小学,初中,高中,大专,本科,硕士,博士)

  H2: 父母儿童时期高图书馆使用 → 子女成年后更可能成为图书馆用户
      指标: child_library_use_rate(子女成年后图书馆使用率)
      条件对比: W3 vs W1
      方向: child_library_use_rate(W3) > child_library_use_rate(W1)
      MDE: +20%

  H3: 父母图书馆使用的影响部分通过家庭文化资本中介
      指标: home_culture_index(家庭文化资本指数,基于藏书量、讨论频次等)
      条件对比: W3 vs W1
      方向: home_culture_index(W3) > home_culture_index(W1)
      MDE: +15%

跑前检查:
  ✓ 所有指标在目录中
  ✓ 调用量预算可接受
  ✓ 仿真时长: 25 年(跨代际)

实验:
  3 条件 × 5 种子 = 15 次仿真
  每个仿真: 1000 个家庭 × 25 年
```

这个预注册涵盖了研究问题、条件、假设、指标、判定规则。它是**仿真开始前**就写好的——跑完之后,只有按这个协议判定的结果才算数。

---

## 9.7　本章小结

- 社会仿真适合:机制识别、反事实评估、长期动态、多变量交互、不可实验问题。
- 预注册协议是社会仿真研究的底线:研究问题、条件、假设、指标、判定规则五件套,跑前写好,跑后严格执行。
- 效应量、MDE、样本量是预注册的关键参数;GAWorld 的默认规则:高频变量 MDE=5%,低频 10%,结构 15%。
- 对照组有三种:对照世界、安慰剂世界、平行世界,功能各异。
- 指标目录是可测量性的底线,假设只能从目录里选指标。
- 一个完整的预注册示例(限行令、公共图书馆)展示了协议的实际形式。

---

## 9.8　思考题

1. **选一个你关心的研究问题**(真实或假设),**判断它是否适合仿真**:是机制识别、反事实评估、长期动态、多变量交互、还是不可实验问题?
2. **为一个研究问题写预注册协议**:包括 1 个研究问题、2–3 个条件、3–5 条假设、每条假设的指标和 MDE。
3. **为限行令案例补充第 4–6 条假设**:除了 H1–H3,你还能想到哪些可被仿真验证的假设?
4. (进阶)**为你的研究估算样本量**:多少个条件 × 多少个种子 = 多少次仿真?总成本多少?

---

## 9.9　延伸阅读

1. Nosek, B. A., et al. (2018). The Preregistration Revolution. *Psychological Science*, 17(2), 28–37. —— 预注册运动的总览。
2. Munafò, M. R., et al. (2017). A Manifesto for Reproducible Science. *Nature Human Behaviour*, 1(1), 0021. —— 可复现科学的纲领。
3. Cohen, J. (1988). *Statistical Power Analysis for the Behavioral Sciences*. Lawrence Erlbaum. —— 效应量的经典教材。
4. List, J. A. (2011). Why Economists Should Conduct Field Experiments and 14 Tips for Pulling One Off. *Journal of Economic Perspectives*, 25(3), 3–16. —— 实验设计的经济学视角。
5. GAWorld 工程文档:`docs/PHASE4_AUDIT.md`、`docs/EXPERIMENTS_REPORT.md`、`proposals/2026-09-19-ai-social-scientist.md`。
6. Ioannidis, J. P. A. (2005). Why Most Published Research Findings Are False. *PLoS Medicine*, 2(8), e124. —— 假阳性率的经典讨论,任何做实证研究的人必读。
7. Gelman, A., & Loken, E. (2014). The Statistical Crisis in Science. *American Scientist*, 102(6), 460–465. —— 统计推断的陷阱。
8. McShane, B. B., et al. (2019). Abandon Statistical Significance. *Nature*, 565(7740), 307–308. —— 关于"统计显著性"局限的讨论。

---

> **本章教学注释**
>
> 这一章是方法层第一章,是社会仿真作为研究方法全流程的第一步。读者如果要做研究,**必须**从预注册协议开始——不能跳过这一步。
>
> 9.6 节"公共图书馆的代际影响"是一个真实的研究问题,这种"长跨度代际研究"是社会仿真最能发力的场景——真人实验几乎不可能跨 25 年。读者如果想从"短时段效应"开始,可以把仿真时长缩到 1–3 年,跑小规模实验,熟悉流程后再做长跨度研究。
>
> 9.5 节"指标目录"是 GAWorld 的核心概念之一。读者做研究时,**必须**确认你要用的指标在目录中;如果不在,要先在仿真里加入对应测量,然后再写预注册。否则就是事后定义指标,违反预注册原则。
>
> 9.2 节的预注册模板来自 GAWorld 的"研究工作台"功能,具体实现见 `gaworld/apps/research_api.py`。下一章讨论"实验设计":从 A/B 到析因到平行世界,怎么把预注册变成可执行的实验。
---

## 9.10　扩展:研究问题的精细化

把模糊的研究问题变成具体的仿真假设,需要经过精细化。本节展开讨论。

### 9.10.1　研究问题的层次

研究问题可以分四层:

- **现象层**:观察到"什么"
- **机制层**:"为什么"会这样
- **政策层**:应该"怎么做"
- **理论层**:这"意味着什么"

仿真最适合"机制层"和"政策层",不适合"现象层"和"理论层"。

### 9.10.2　问题分解

一个模糊的问题可以分解为多个具体的子问题:

```
[原始问题]为什么社会贫富差距持续扩大?

[分解后]
  - 问题 1:代际财富传递的机制是什么?
  - 问题 2:教育机会不平等如何影响财富积累?
  - 问题 3:政策干预(累进税、教育补贴)效果如何?
```

每一子问题可以做 1 个独立仿真。

### 9.10.3　假设的"形式化"

把假设写成"可证伪"的形式:

```
模糊:教育很重要
形式化:接受高等教育使 30 岁时收入提高 ≥ 30%(MDE 30%)
```

### 9.10.4　假设的反例

每个假设都应该能想到反例。写假设时,同时写"在什么条件下假设会失败":

```
H1: 限行减少私家车出行
反例 1:如果居民没有私家车,效应接近 0
反例 2:如果公共交通已饱和,居民不会转向公交
反例 3:如果弹性工作制普及,出行总量会下降而非出行方式转移
```

---

## 9.11　扩展:预注册协议的细节

预注册协议的每个字段都有讲究。本节展开讨论。

### 9.11.1　指标的定义

指标必须明确"怎么算":

```
模糊指标:经济活跃度
明确指标:人均每月消费金额(元),基于 daily_consumption 加总,排除房租
```

### 9.11.2　条件的精确性

每个条件必须有明确的差异:

```
模糊:高补贴 vs 低补贴
精确:高补贴 = 公共交通票价 50% 折扣;低补贴 = 无补贴
```

### 9.11.3　方向的明确性

方向必须明确:

```
模糊:减少
明确:实施后均值 < 实施前均值的 90%
```

### 9.11.4　MDE 的合理性

MDE 必须基于真实数据估计:

```
方法:用预实验跑 30 天,看指标波动范围,MDE = 2 × 波动范围
```

### 9.11.5　判定规则的完整性

每条假设必须有明确的判定规则:

```
H1: 限行减少私家车出行 ≥ 15%
判定:实施后均值的 95% CI 上界 ≤ 实施前均值的 85% → supported
判定:实施后均值的 95% CI 下界 ≥ 实施前均值 → inconclusive
判定:实施后均值的 95% CI 上界 > 实施前均值 → contradicted
```

---

## 9.12　扩展:从假设到仿真的转换

把假设转换成仿真参数。

### 9.12.1　参数化

```python
HYPOTHESIS_TO_CONFIG = {
    "H1_limit_reduces_car_trips": {
        "worlds": {
            "control": {"events": []},
            "treatment": {"events": ["limit_weekday"]}
        },
        "metric": "car_trips_per_capita",
        "mde": -0.15,
        "direction": "lower"
    }
}
```

### 9.12.2　预运行

跑短时段验证流程:

```bash
python generative_city_sim.py run --sim-days 7 --seed 42 --dry-run
```

如果流程跑通,可以扩展到正式实验。

### 9.12.3　参数微调

根据预运行调整参数:

- 如果差异太小,加大样本量
- 如果差异太大,缩小样本量
- 如果有异常,调整算法

### 9.12.4　正式运行

```bash
python generative_city_sim.py parallel-worlds --spec spec.json --output output/case
```

---

## 9.13　扩展:假设检验的统计学

仿真研究里的假设检验比传统统计复杂。

### 9.13.1　样本量的确定

仿真里的"样本量"是"种子数 × 条件数":

- 太少(<3):噪声主导,结论不稳
- 太多(>10):成本飙升
- 推荐:3 条件 × 5 种子 = 15 次仿真

### 9.13.2　效应量与 MDE

效应量 = 真实效应大小。MDE = 愿意接受的最小效应。两者关系:

- MDE < 效应量:能检测到
- MDE > 效应量:检测不到

经验法则:MDE = 效应量的 50%–80%。

### 9.13.3　多重比较校正

做多个假设检验时,需要校正显著性水平:

```
原始 α = 0.05
Bonferroni 校正:α' = 0.05 / k
其中 k = 假设数
```

### 9.13.4　事前 vs 事后分析

**事前分析**(a priori):在仿真前就确定,严格按计划执行。

**事后分析**(post hoc):跑完仿真后再选择,通常是"看到想要的结果"。

预注册只允许事前分析。事后分析只能作为"探索性发现",不能作为"假设检验"。

---

## 9.14　扩展:研究问题的来源

### 9.14.1　从现实问题来

最常见的研究问题来源:

- 政策争议(限行、税收、福利)
- 社会现象(贫富差距、舆论极化)
- 历史事件(金融危机、疫情)

### 9.14.2　从理论来

从既有理论找空白:

- "X 理论预测 Y,但实证不支持"
- "X 机制如何与 Y 机制交互?"
- "X 假设在 Z 情境下还成立吗?"

### 9.14.3　从数据来

从数据异常找问题:

- "为什么这个变量不符合预期?"
- "为什么这两个变量的关系是反的?"

### 9.14.4　从跨学科来

从其他学科找灵感:

- 物理学(复杂系统、网络科学)→ 社会科学
- 生物学(生态系统、传染病)→ 社会科学
- 经济学(博弈论、机制设计)→ 政策仿真

---

## 9.15　本章小结(扩展版)

- 研究问题的精细化:分层、分解、形式化、反例。
- 预注册协议的细节:指标、条件、方向、MDE、判定规则。
- 从假设到仿真的转换:参数化、预运行、微调、正式运行。
- 假设检验的统计学:样本量、效应量、多重比较、事前/事后。
- 研究问题来源:现实、理论、数据、跨学科。


---

## 9.16　扩展:研究问题的精细化练习

为了帮助读者把模糊问题变成具体假设,本节给出三个完整练习。

### 练习一:教育公平问题

**模糊问题**:"教育资源不平等对代际流动有什么影响?"

**精细化**:

```
研究问题: 城乡教育资源不平等如何影响代际收入流动?
假设:
  H1: 农村学生接受高等教育的概率显著低于城市学生
      指标: higher_education_rate | residence = rural
            higher_education_rate | residence = urban
      方向: 农村 < 城市
      MDE: -20%

  H2: 教育资源不平等部分通过"早期学业表现"中介
      指标: early_achievement_score | school_quality
      方向: 学校质量越高,早期学业表现越好
      MDE: +15%

  H3: 政策干预(教育补贴)能缩小城乡差距
      指标: higher_education_rate_diff | subsidy = true
            higher_education_rate_diff | subsidy = false
      方向: 补贴缩小差距
      MDE: -10%
```

### 练习二:医疗资源问题

**模糊问题**:"医疗资源分布对健康公平有什么影响?"

**精细化**:

```
研究问题: 基层医疗资源不足如何影响老年人健康管理?
假设:
  H1: 基层医疗资源不足导致老年人慢性病管理率低
      指标: chronic_disease_management_rate | primary_care = 充足
            chronic_disease_management_rate | primary_care = 不足
      方向: 充足 > 不足
      MDE: +15%

  H2: 医疗资源不足导致老年人急诊使用率增加
      指标: emergency_use_rate | primary_care = 不足
      方向: 增加
      MDE: +10%

  H3: 家庭医生签约制度能缓解医疗资源不足的影响
      指标: chronic_management_rate | signed = true
      方向: 签约 > 未签约
      MDE: +20%
```

### 练习三:环境治理问题

**模糊问题**:"环保政策对企业行为有什么影响?"

**精细化**:

```
研究问题: 碳排放政策对企业绿色投资决策有什么影响?
假设:
  H1: 碳税政策促使企业增加绿色投资
      指标: green_investment_rate | carbon_tax = true
            green_investment_rate | carbon_tax = false
      方向: 增加
      MDE: +25%

  H2: 不同规模企业对碳税政策的反应不同
      指标: green_investment_rate | firm_size = large
            green_investment_rate | firm_size = small
      方向: 大企业反应更强
      MDE: +20%
```

---

## 9.17　扩展:从假设到实验设计的转换

### 9.17.1　假设 → 条件的映射

每条假设对应一组条件:

```python
HYPOTHESIS_CONDITION_MAP = {
    "H1": {
        "conditions": ["control", "treatment"],
        "key_difference": "carbon_tax",
        "key_metric": "green_investment_rate"
    },
    "H2": {
        "conditions": ["large_firm", "small_firm"],
        "key_difference": "firm_size",
        "key_metric": "green_investment_rate"
    }
}
```

### 9.17.2　假设 → 指标的校验

每个假设的指标必须在可测量目录中:

```python
def validate_metrics(hypotheses: list, metrics_catalog: list) -> bool:
    """校验指标是否在目录中"""
    catalog_names = {m["name"] for m in metrics_catalog}
    for h in hypotheses:
        if h["metric"] not in catalog_names:
            return False
    return True
```

### 9.17.3　假设 → 调用的预算

每个假设需要的 LLM 调用:

```python
def estimate_call_budget(hypotheses: list, agents: int,
                        days: int) -> dict:
    """估算 LLM 调用预算"""
    return {
        "total_calls": len(hypotheses) * agents * days,
        "estimated_cost_usd": ...,
        "estimated_time_hours": ...
    }
```

---

## 9.18　扩展:预注册的工程实现

### 9.18.1　预注册的版本管理

```python
PREREG_VERSIONS = {
    "v1": {
        "conditions": [...],
        "hypotheses": [...],
        "metrics": [...],
        "judge_rules": [...]
    },
    # 修改后,新建版本
    "v2": {...}
}
```

### 9.18.2　预注册的对比分析

如果跑前修改了预注册,要明确记录:

```python
def diff_prereg_versions(v1: dict, v2: dict) -> list:
    """两个预注册版本的差异"""
    changes = []
    # 比较条件
    if v1["conditions"] != v2["conditions"]:
        changes.append("conditions changed")
    # 比较假设
    ...
    return changes
```

### 9.18.3　预注册的不可变性

预注册一旦批准,不可修改。如果必须修改:

- 创建新版本
- 旧版本保留作历史
- 论文里报告"为什么修改"

---

## 9.19　扩展:研究问题的来源与灵感

### 9.19.1　从现实问题来

最常见的研究问题来源:

- 政策争议(限行、税收、福利)
- 社会现象(贫富差距、舆论极化)
- 历史事件(金融危机、疫情)

### 9.19.2　从理论来

从既有理论找空白:

- "X 理论预测 Y,但实证不支持"
- "X 机制如何与 Y 机制交互?"
- "X 假设在 Z 情境下还成立吗?"

### 9.19.3　从数据来

从数据异常找问题:

- "为什么这个变量不符合预期?"
- "为什么这两个变量的关系是反的?"

### 9.19.4　从跨学科来

从其他学科找灵感:

- 物理学(复杂系统、网络科学)→ 社会科学
- 生物学(生态系统、传染病)→ 社会科学
- 经济学(博弈论、机制设计)→ 政策仿真

---

## 9.20　本章小结(最终扩展版)

- 研究问题的精细化需要层层分解,从模糊到具体。
- 预注册协议每个字段都有讲究,严格定义。
- 从假设到实验的转换需要参数化、校验、预算估算。
- 预注册的版本管理、对比分析、不可变性。
- 研究问题来源:现实、理论、数据、跨学科。

