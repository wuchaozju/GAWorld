GAWorld本地运行结果&问题：
1、已加入使用nuwa蒸馏出的我自己的数字分身，即agent52（王思颖），具体信息见csv和md文件；用的是自己写过的文档蒸馏，最近导出微信聊天记录的项目和第三方工具好像被管得很严（很多库都被删了），没成功导出来。
2、运行仿真时使用5个agent模拟3天（并发设置为5），其中包含agent52。
3、多次测试运行发现仿真会卡在第一天，不确定是项目代码本身有问题还是确实运行的慢，通常一天没跑完api就限额了（哪怕只用一个agent仿真也会一直停在第一天）。
4、断点续跑时会从头开始，导致时间戳乱序。
5、最后一次的运行结果已存在output文件夹，没跑完，卡在了第一天。
