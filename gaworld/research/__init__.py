"""研究工作台 — turn a research idea or a paper into a GAWorld implementation plan,
then carry it through as a study: pre-registered protocol → parallel worlds run →
computed verdicts → model reading → report (``measures`` / ``protocol`` /
``backends`` / ``evaluate`` / ``interpret`` / ``report`` / ``study``)."""
